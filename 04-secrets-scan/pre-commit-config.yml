# Установка:
#   pip install pre-commit
#   pre-commit install
# Теперь при каждом git commit автоматически проверяет на секреты

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
